To test, run the application from the top directory:

$ ./gradlew :sample:bootRun

Once the server is running, run this command:

$ curl localhost:8080/meow

If everything works correctly, you should receive some cat facts as text output.

